# معماری همگام‌سازی پنگ ۲.۰

## وضعیت فعلی

پنگ ۲.۰ در وضعیت فعلی فقط محلی است و همگام‌سازی مرکزی ندارد.

## طراحی آینده

حالت‌های پیشنهادی:

```text
local-only
queued
uploading
synced
failed
conflict
revoked
```

همگام‌سازی باید اختیاری، HTTPS، قابل ادامه، دارای checksum و تأیید سرور باشد. تشخیص تکرار بر اساس شناسه پایدار بازدید انجام می‌شود. داده محلی بلافاصله بعد از upload حذف نمی‌شود. اعلان Push فقط وجود فرمان یا کار جدید را اعلام می‌کند و نباید حاوی داده حساس باشد.
