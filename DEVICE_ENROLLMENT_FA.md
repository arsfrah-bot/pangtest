# معماری ثبت دستگاه در پنگ ۲.۰

## وضعیت فعلی

شناسه نصب محلی و پایدار ایجاد می‌شود. ثبت سروری و ایمیل خودکار پیاده‌سازی نشده‌اند.

## طراحی آینده

ثبت دستگاه باید شفاف، با اطلاع کاربر و از طریق HTTPS انجام شود. پیام ثبت شامل شناسه نصب، کد نمایندگی، نسخه برنامه و زمان است؛ رمز، کد ملی، عکس و اطلاعات بازدید نباید ارسال شوند.

ارسال ایمیل مستقیم از PWA یا APK ممنوع است؛ زیرا نیازمند قرار دادن رمز SMTP یا کلید سرویس در برنامه خواهد بود. ایمیل فقط باید توسط backend امن و پس از ثبت یکتای دستگاه ارسال شود.

حالت‌های پیشنهادی:

```text
local-only
pending
enrolled
failed
revoked
```
