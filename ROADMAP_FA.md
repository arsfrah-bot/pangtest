# نقشهٔ راه پنگ

نام رسمی محصول از نسخهٔ ۲.۰ «پنگ» است.

> پنگ؛ یک هسته، صدها شاخه، یک نظم

نام نمایشی لاتین:

```text
PANG
```

«بازدیدیار» نام پیشین و میراثی نسخه‌های ۱.۹ و قبل از آن است. نام‌های فنی وابسته به آن تا زمان طراحی و آزمون مهاجرت، حفظ می‌شوند.

## فعال در نسخهٔ ۲.۰

۱. هویت مادر پنگ — نام، نشان، شعار، آیکون، ردپای خروجی و نام‌گذاری ساخت در نسخهٔ ۲.۰ اعمال شده است.

۲. بازدید اولیه بدنهٔ خودرو — شاخهٔ عملیاتی پایدار و نیازمند ادامهٔ آزمون میدانی.

۳. بازدید اولیه آتش‌سوزی — شاخهٔ آزمایشی پیاده‌سازی‌شده در نسخهٔ ۱.۸؛ نیازمند تأیید فرم توسط کارشناس شعبه و آزمون پروندهٔ واقعی.

۴. زیرساخت موتور پویای شرایط ویژه — پایهٔ فنی نسخهٔ ۱.۹؛ کاتالوگ‌ها و موتور آزموده شده‌اند، اما هنوز جایگزین فرم‌های عملیاتی نیستند.

## خط مبنای ساخت نسخهٔ ۲.۰

گردش‌کار رسمی ساخت آزمایشی:

```text
Build PANG APK - Easy Debug
```

بستهٔ خروجی:

```text
PANG-Insurance-Platform-v2.0-debug-apk
```

فایل نصب:

```text
PANG-Insurance-Platform-v2.0-debug.apk
```

## گام بعدی پیشنهادی

- اجرای گردش‌کار نسخهٔ ۲.۰ و نصب فایل حاصل روی گوشی واقعی
- آزمون ارتقا از نسخهٔ ۱.۹ به ۲.۰ بدون حذف داده و بدون نصب جداگانه
- کنترل دسترسی به بایگانی، پیش‌نویس‌ها و پشتیبان‌های تولیدشده با نام پیشین
- کنترل بصری نشان، نام فارسی، شعار و آیکون در اندازه‌ها و تراکم‌های مختلف نمایشگر
- کنترل نام پوشهٔ دانلود و نام فایل‌های خروجی خودرو و آتش‌سوزی
- سپس تبدیل ماژول خودرو به نخستین طرح‌وارهٔ پویا، همراه با مقایسهٔ کامل رفتار و خروجی با نسخهٔ پایدار
- پس از تثبیت خودرو، انتقال آتش‌سوزی مسکونی و غیرصنعتی به همان موتور پویا

## قرارداد مهاجرت

شناسه‌های زیر بخشی از قرارداد سازگاری‌اند و تغییر آن‌ها یک کار مستقل با مهاجرت نسخه‌دار، پشتیبان و مسیر بازگشت است:

```text
ir.bimehiran.bazdidyar
bazdidyar_pwa
bazdidyar_db
bazdidyar_profile
bazdidyar_installation_id
bazdidyar-backup
BazdidyarDomain
BazdidyarCoverageEngine
BazdidyarFoundationInfo
bazdidyar.local
bazdidyar-domain-manifest
bazdidyar-vehicle-module
```

## برنامه‌ریزی‌شده و غیرعملیاتی

- ثبت شفاف دستگاه و ارسال اعلان ثبت به سرور امن
- همگام‌سازی اختیاری و داشبورد مدیر
- فرم‌ساز مدیریتی و انتشار نسخه‌های تأییدشدهٔ کاتالوگ
- نرخ و فرانشیز رسمی پس از دریافت جدول یا بخشنامهٔ معتبر
- بازدید اولیهٔ مسئولیت
- رابط عملیاتی کارشناسی خسارت آتش‌سوزی و مسئولیت
- برنامهٔ مهاجرت کنترل‌شده برای شناسه‌های میراثی، فقط در صورت وجود نیاز فنی واقعی

کارت یا فرم نیمه‌کاره برای ماژول‌های غیرعملیاتی در نسخهٔ جاری نمایش داده نمی‌شود.
