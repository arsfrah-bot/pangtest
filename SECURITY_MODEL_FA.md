# مدل امنیتی پنگ ۲.۰

## پیاده‌سازی‌شده

- عملکرد پیش‌فرض آفلاین و بدون upload
- هش رمز محلی PWA و حذف رمز از backup
- شناسه تصادفی با Web Crypto
- WebViewAssetLoader به‌جای file URL
- ممنوعیت دسترسی عمومی فایل محلی و mixed content
- Safe Browsing و بازشدن لینک خارجی در برنامه سیستم
- FileProvider، scoped storage و پل محدود دانلود/اشتراک

## محدودیت

PWA در برابر دسترسی کامل به دستگاه یا پروفایل مرورگر امنیت سخت‌افزاری ایجاد نمی‌کند. backup رمزگذاری نشده است. بررسی امنیت مستقل و آزمون گوشی واقعی پیش از بهره‌برداری گسترده لازم است.

## نیازمند backend

ثبت دستگاه، ایمیل، احراز هویت مدیر، همگام‌سازی، لغو نشست، فرمان راه دور و audit log.

هیچ کلید API، رمز SMTP، keystore یا secret نباید در مخزن یا frontend قرار گیرد.
