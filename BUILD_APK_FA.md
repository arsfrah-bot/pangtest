# راهنمای ساخت فایل نصب پنگ — نسخهٔ ۲.۰

نام رسمی محصول:

> پنگ

شعار رسمی:

> پنگ؛ یک هسته، صدها شاخه، یک نظم

نام نمایشی لاتین:

```text
PANG
```

## روش پیشنهادی: ساخت آزمایشی با گیت‌هاب

برای این روش، نصب محلی ابزارهای جاوا یا اندروید لازم نیست. محتوای پروژه را در مخزن بارگذاری کنید، بخش اقدامات را فعال کنید و گردش‌کار زیر را دستی اجرا کنید:

```text
Build PANG APK - Easy Debug
```

نام فایل گردش‌کار:

```text
.github/workflows/build-apk.yml
```

بسته‌ای که در بخش خروجی‌ها نمایش داده می‌شود:

```text
PANG-Insurance-Platform-v2.0-debug-apk
```

فایل نصب داخل بسته:

```text
PANG-Insurance-Platform-v2.0-debug.apk
```

نسخهٔ برنامه در تنظیمات اندروید:

```text
versionCode 20
versionName 2.0-pang-brand-foundation
```

پیش از ساخت، وظیفهٔ زیر فایل‌های نسخه‌دار پوشهٔ دامنه را به دارایی‌های تولیدشدهٔ برنامه منتقل می‌کند:

```text
syncDomainAssets
```

فایل تنظیمات زیر باید در ریشهٔ مخزن باشد؛ نبود آن می‌تواند مرحلهٔ کنترل فرادادهٔ کتابخانه‌های اندروید را با شکست مواجه کند:

```text
gradle.properties
```

## ساخت محلی اختیاری

در صورت نصب نسخهٔ ۱۷ محیط جاوا و ابزار توسعهٔ اندروید:

```powershell
gradle assembleDebug --no-daemon
```

## امضای نسخهٔ انتشار — اختیاری

این مسیر برای آزمون فعلی اجباری نیست. کلید امضا را یک‌بار بسازید و هرگز آن را عمومی یا داخل مخزن ثبت نکنید.

نام فایل نمونهٔ قدیمی، برای سازگاری با دستورالعمل‌های نسخه‌های پیشین حفظ شده است:

```powershell
keytool -genkeypair -v -keystore bazdidyar-release.jks -alias bazdidyar -keyalg RSA -keysize 2048 -validity 10000
```

متغیرهای محیطی لازم:

```text
ANDROID_KEYSTORE_PATH
ANDROID_KEYSTORE_PASSWORD
ANDROID_KEY_ALIAS
ANDROID_KEY_PASSWORD
```

فرمان ساخت نسخهٔ انتشار:

```powershell
gradle assembleRelease
```

نگهداری امن فایل کلید و گذرواژه‌ها برای به‌روزرسانی نسخه‌های آینده ضروری است.

## سازگاری با نام پیشین

«بازدیدیار» نام پیشین و میراثی محصول است. شناسهٔ بسته، فضای نام جاوا، مسیر دارایی‌های وب و شماری از کلیدهای داده عمداً تغییر نکرده‌اند تا ارتقا به نسخهٔ ۲.۰ دسترسی به نصب و بایگانی قبلی را قطع نکند.

```text
ir.bimehiran.bazdidyar
bazdidyar_pwa
bazdidyar_db
bazdidyar_profile
bazdidyar-backup
```

این شناسه‌ها نباید صرفاً برای یکسان‌شدن با نام برند جدید تغییر کنند. هر تغییر آینده به مهاجرت داده، آزمون ارتقا و برنامهٔ بازگشت نیاز دارد.

ردپای فعلی:

> پنگ | ایده، طراحی و توسعه: فاضل رحمانیان | بیمه ایران شعبه جهرم

ردپای تاریخی نسخه‌های پیشین:

> بازدیدیار بدنه خودرو | ایده، طراحی و توسعه: فاضل رحمانیان | بیمه ایران شعبه جهرم
