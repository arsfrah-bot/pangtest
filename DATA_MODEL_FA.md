# مدل داده پنگ ۲.۰

## پایگاه محلی

نام پایگاه:

```text
bazdidyar_db
```

نسخه schema برابر ۲ است.

- `inspections`: رکوردهای نهایی با کلید عددی `id` و شناسه تجاری پایدار `reportNo`.
- `drafts`: پیش‌نویس فعال با کلید `active`.
- `meta`: برای توسعه مهاجرت و فراداده آینده.

رکورد نهایی شامل مشخصات نمایندگی، بیمه‌گزار، پلاک نمایشی و نرمال‌شده، عکس‌های PDF/بایگانی/thumbnail، وضعیت `finalized` و فراداده PDF و ZIP است.

پیش‌نویس هرگز نباید رکورد نهایی را بازنویسی کند. بازیابی پشتیبان رکوردهای دارای کلید یکسان را تکراری محسوب و رد می‌کند.

## رکورد آتش‌سوزی

رکوردهای آتش‌سوزی دارای مقدار زیر هستند:

```text
module: fire
```

این رکورد شامل اطلاعات محل، ساختمان، سرمایه‌های تفکیک‌شده، ارزیابی‌های ایمنی، عکس‌های سه‌کیفیتی و فراداده خروجی است. پیش‌نویس مستقل آن با کلید زیر ذخیره می‌شود:

```text
fire-active
```

## دارایی‌های نسخه‌دار دامنه

کاتالوگ‌ها و بسته‌های موتور پویا در پوشه `domain` نگهداری می‌شوند و در این مرحله داخل IndexedDB نوشته نمی‌شوند. فایل `domain/manifest.json` فهرست ۳۲ دارایی اجرایی را تعیین می‌کند. طرح‌واره‌های کنترلی جداگانه در `domain/schemas` قرار دارند.

هر دارایی اجرایی دارای شناسه پایدار و نسخه است. پرونده‌های پویا در مرحله مهاجرت باید علاوه بر داده کاربر، شناسه و نسخه محصول، فعالیت، پوشش، شرط، بسته خسارت، بسته عکس و قالب گزارش استفاده‌شده را ذخیره کنند.

## نتیجه ارزیابی پوشش

نتیجه موتور شامل پوشش و نسخه، واقعیت‌های محرک، شواهد مؤید، مدارک مفقود، استثناهای احتمالی، ارجاع قاعده فرانشیز، تکالیف بیمه‌گذار و وضعیت تصمیم است. تأیید یا رد نهایی بدون اطلاعات کاربر انسانی مجاز معتبر محسوب نمی‌شود.

## سازگاری رکوردهای قدیمی

رکوردهای خودرو و آتش‌سوزی نسخه‌های قبلی مهاجرت اجباری نمی‌شوند. موتور دامنه در پنگ ۲.۰ زیرساخت جانبی است و ساختار IndexedDB نسخه ۲ فعلی را تغییر نمی‌دهد؛ بنابراین بایگانی موجود قابل خواندن باقی می‌ماند.
