# ممیزی خط مبنای تاریخی پنگ ۲.۰

این سند منابع دوره پیش از نام‌گذاری پنگ را ثبت می‌کند. نام بسته‌ها، ماژول‌ها و نسخه‌های تاریخی برای ردیابی فنی بدون تغییر حفظ شده‌اند.

## بسته‌های بررسی‌شده

هر ۹ بسته اعلام‌شده بررسی و استخراج شد: چهار بسته PWA، چهار بسته سازنده APK و پروژه قدیمی Android نسخه ۱.۰.۱.

## معماری واقعی

- هسته فعال: PWA آفلاین فارسی با HTML، CSS و JavaScript، ذخیره‌سازی IndexedDB و تولید PDF/ZIP در دستگاه.
- بسته Android: پوسته Java WebView با پل محدود دانلود و اشتراک‌گذاری.
- ساخت: Gradle و GitHub Actions، بدون نیاز به Android Studio برای Debug.
- پروژه ۱.۰.۱: شاخه قدیمی Android و منبع قابلیت‌های فعلی نیست.

## مقایسه

- `pwa_no_android_studio`: هسته اولیه و کم‌امکانات.
- `pwa_v1_1_fixed`: اصلاح فرم و خروجی.
- `pwa_v1_2_photo_archive`: بایگانی عکس با کیفیت بالاتر.
- `pwa_v1_3_zip_trace`: کامل‌ترین PWA تاریخی؛ ZIP و ردپای مالکیت.
- چهار بسته Android از ۱.۳ تا ۱.۵: فایل PWA یکسان با پل Android؛ تفاوت اصلی در ساخت و امضای Release است.
- نسخه ۱.۶ اصلاح‌شده: مبنای عملی این ارتقا، شامل رفع AndroidX و اصلاحات WebView/ZIP قبلی.

## خط مبنای انتخاب‌شده

هسته قابلیت‌ها از PWA ۱.۳ و پوسته ساخت از APK ۱.۵ گرفته شده بود؛ نسخه ۱.۶ اصلاح‌شده، ادغام سالم آن‌هاست و مبنای مستقیم ۱.۷ شد.

## موارد کنار گذاشته‌شده

هیچ فایل اصلی کاربر حذف نشد. نسخه‌های قدیمی فقط به‌عنوان مرجع ممیزی استفاده شدند و روی نسخه جدید بازنویسی نشدند.

## ریسک‌های سازگاری

- حجم زیاد عکس‌ها می‌تواند سقف ذخیره مرورگر را پر کند.
- امنیت رمز محلی PWA هم‌سطح Android Keystore نیست.
- اشتراک فایل در مرورگر به پشتیبانی دستگاه وابسته است.
- APK ۱.۷ باید یک‌بار روی گوشی واقعی برای دوربین، انتخاب JSON، دانلود و Share Intent آزمایش شود.
